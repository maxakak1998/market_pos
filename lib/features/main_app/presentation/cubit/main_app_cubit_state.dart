import '../../../../app_export.dart';

class MainAppInitState extends BaseCubitState {
  MainAppInitState({
    super.state,
    super.error,
    super.id,
    super.isLoadMore,
    super.isRefresh,
  });
}

class ChangeThemeState extends BaseCubitState {
  ChangeThemeState({
    super.state,
    super.error,
    super.id,
    super.isLoadMore,
    super.isRefresh,
  });
}
